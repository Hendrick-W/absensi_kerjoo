import React from 'react';
import { Text, View } from 'react-native';

const MapPage = ({
    params,
}) => {
  return (
    <View>
      <Text>MapPage</Text>
    </View>
)};

export default MapPage;
